apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tfo-collector.configMapName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tfo-collector.labels" . | nindent 4 }}
data:
  tfo-collector.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
