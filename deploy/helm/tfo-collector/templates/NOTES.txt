TFO-Collector has been deployed!

  Replicas: {{ .Values.replicaCount }}
  Image:    {{ include "tfo-collector.image" . }}

TFO Platform endpoint: {{ .Values.telemetryflow.endpoint | default "(not set — update telemetryflow.endpoint)" }}

OTLP Endpoints (in-cluster):
  gRPC:  {{ include "tfo-collector.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:4317
  HTTP:  {{ include "tfo-collector.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:4318

  v1 (open):            POST /v1/traces | /v1/metrics | /v1/logs
  v2 (authenticated):   POST /v2/traces | /v2/metrics | /v2/logs
    Headers: X-TelemetryFlow-Key-ID, X-TelemetryFlow-Key-Secret

To check collector status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "tfo-collector.name" . }}

To view logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "tfo-collector.name" . }} --tail=50

To check the health endpoint:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "tfo-collector.fullname" . }} 13133:13133
  curl http://localhost:13133/
